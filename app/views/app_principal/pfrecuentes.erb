<% content_for :contenido do %>
<div class="container xsx-width">
  <!-- ===== Begin All Sections ===== -->
  <div class="">
    <div class="intro_faq">
      <h3>Preguntas Frecuentes</h3>
      <p>Éstas son las preguntas que nuestros clientes hacen con más frecuencia, si su pregunta no se encuentra en este listado, siéntase libre de enviarnos un mensaje mediante el formulario de contacto en el menú principal del sitio.</p>
    </div>

    <div class="panel-group" id="accordion_1">
      <% @cont = 0 %>
      <% @preguntas.each do |p| %>
          <% @cont += 1 %>
          <div class="panel panel-default">
            <div class="panel-heading">
              <h4 class="panel-title">
                <a class="accordion-toggle
                      <% if @cont != 1 then %>collapsed<%end%>" data-toggle="collapse" data-parent="#accordion_1" href="#collapse_<%= @cont %>">


                  <span class="preguntatxt"><%= p.pregunta %></span>

                </a>
              </h4>
            </div>
            <div id="collapse_<%= @cont %>" class="panel-collapse collapse <% if @cont == 1 then %>in<%end%>" style="<% if @cont != 1 then %>height: 0px;<%end%>">
              <div class="panel-body">
                <span class="respuestatxt"><%= p.respuesta %></span> </a>
              </div>
            </div>
          </div>


      <% end %>

    </div>
    </div>

  </div>
<% end %>

<%= render template: "layouts/usuariobase" %>